# README screenshots

The README references two screenshots of the **Code** tab in Claude Desktop. Drop the two PNGs
here **with exactly these names**:

| Expected file | Screenshot content |
|---|---|
| `desktop-folder-chips.png` | The bottom of a **New session**: the row of chips `💻 Local · 📁 tmp · ➕`, just above the input field. |
| `desktop-recent-menu.png` | The **"Recent"** menu opened after clicking the folder chip: the list of recent folders with the **✓** on the current folder, and **"Open folder…"** at the bottom. |

Until these files are present, the two README images will appear broken.

## Notion-token guide screenshots

[`docs/notion-token-setup.md`](../notion-token-setup.md) references six screenshots of the **English**
Notion UI. Drop the PNGs here **with exactly these names**:

| Expected file | Screenshot content |
|---|---|
| `notion-token-01.png` | The integrations page at `notion.so/my-integrations` (signed in). |
| `notion-token-02.png` | The **“+ New integration”** button. |
| `notion-token-03.png` | The new-integration form: **Internal** type, associated workspace, a name. |
| `notion-token-04.png` | The **“Internal Integration Secret”** field with **Show / Copy** (value blurred). |
| `notion-token-05.png` | A page's **•••** menu → **Connections** → selecting the integration. |
| `notion-token-06.png` | The `.env` file open, the token pasted after `NOTION_TOKEN_…=` (value blurred). |

Until these files are present, the guide's images will appear broken.
